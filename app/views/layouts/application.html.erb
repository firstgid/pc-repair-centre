<!DOCTYPE html>
<html>
<head>
  <title><%= full_title yield(:title) %></title>
  <link href='https://fonts.googleapis.com/css?family=Jura&subset=latin,latin-ext' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <meta name="Author" content="Maciej Dziurzyński" />
  <%= render 'layouts/shim'%>
  <%= csrf_meta_tags %>
</head>
<body style="font-family: Jura, FreeSans, Arial, sans-serif;">
  <%= render 'layouts/header'%>
  <div class="container">
    <section>
      <%= yield %>
    </section>
    <%= render 'layouts/footer'%>
  </div>
</body>
</html>
